---
# Node Exporter version and architecture
node_exporter_version: "1.8.2"
node_exporter_arch: "linux-amd64"

# Installation paths
node_exporter_install_dir: "/usr/local/bin"
node_exporter_config_dir: "/etc/node_exporter"

# Service configuration
node_exporter_service_user: "root"
node_exporter_service_group: "root"

# Certificate configuration
node_exporter_cert_folder: "/etc/node_exporter/certs"
node_exporter_cert_filename: "node_exporter"

# Network configuration
node_exporter_listen_address: "0.0.0.0:9100"

# Enabled collectors (customize as needed)
node_exporter_enabled_collectors:
  - systemd
  - processes

# Disabled collectors (customize as needed)
node_exporter_disabled_collectors: []

# Nginx Prometheus Exporter (optional)
node_exporter_nginx_enabled: false
node_exporter_nginx_exporter_version: "1.3.0"
node_exporter_nginx_exporter_arch: "linux-amd64"
node_exporter_nginx_stub_status_url: "http://127.0.0.1:8080/stub_status"
node_exporter_nginx_exporter_port: "9113"
node_exporter_nginx_exporter_listen_address: "0.0.0.0:9113"
